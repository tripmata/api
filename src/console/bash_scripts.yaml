# register one or more bash scripts and give them an alias
# you can trigger script by calling php assist 'alias-name' [args]
start-rabbitmq-worker: '%MOOREXA_INSTALLER_DIR%/source/console/rabbitmq-worker.php'